<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LampControl" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/prim</item>
        <item name="android:windowBackground">@color/prim</item>
        <item name="android:navigationBarColor">@color/prim</item>

    </style>

    <style name="Theme.LampControl" parent="Base.Theme.LampControl" />
</resources>